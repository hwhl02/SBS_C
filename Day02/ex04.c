#include <stdio.h>

int main(void) {
    //문자열
    //char 변수명 [문자열 길이+1] = "문자열"; 여러글자를 저장하기 위해서 []를 사용
    char fruit[6]="apple";

    printf("과일이름 : %s\n",fruit);
    // 배열
    // : 여러 자료를 묶어서 사용하는 자료형 

    return 0;
}