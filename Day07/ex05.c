#include <stdio.h>

//함수 : 일련의 기능을 하는 코드(실행문)의 모음

//함수 정의
//반환타입 함수명 (매개변수1, 매개변수2,...){ 실행문 }
int sum(int a, int b){
    int sum=a + b;
    return sum;
    //return(값);
    //1. 함수를 종료
    //2. (값)을 함수를 호호출한 자리로 반환
}
 

int main(void) {
    int a = 10., b= 20;
    void funI()
    // 함수호출 
    // 함수명(전달인자1, 전달인가2);


// 참조에 대한 전당
// : 변수의 값을 전달하는게 아니라, 
// 해당 변ㄴ수에 접근후 수 있느 주소값을절달]ㄴ

   printf("func() 함소출")
    printf("a : %d, b:%d, a,b);
    printf("result : %d\n", result);

    return 0;
}